# Backend-Next Src AGENTS

This directory groups future source sets for `backend-next/`.

## Rules

- Keep source-set responsibilities explicit.
- Do not place business classes directly under `src/`.
- Structural work here should preserve the target package map under `src/main/java`.
