# Files Group AGENTS

This directory groups file-related bounded contexts.

## Rules

- Keep `workspace`, `content`, `upload`, `sharing`, and `search` as separate truths.
- Do not collapse all file behavior back into a single mixed package.
