# Files Search Internal AGENTS

This directory only groups `web`, `application`, `domain`, and `infra`.

## Rules

- Internal search packages are private to this module.
- Do not place loose classes directly in this grouping directory.
