# Files Workspace Internal AGENTS

This directory only groups `web`, `application`, `domain`, and `infra`.

## Rules

- Internal workspace packages are private to this module.
- Do not place stray classes directly in this directory.
