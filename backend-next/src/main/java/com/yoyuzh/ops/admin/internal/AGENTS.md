# Ops Admin Internal AGENTS

This directory only groups `web`, `application`, `domain`, and `infra`.

## Rules

- Internal admin types are not for cross-module imports.
- Do not place direct executable classes in this directory.
