# Identity Access Internal AGENTS

This directory only groups `web`, `application`, `domain`, and `infra`.

## Rules

- Do not place executable classes directly in this directory.
- Internal types here are not for external module imports.
