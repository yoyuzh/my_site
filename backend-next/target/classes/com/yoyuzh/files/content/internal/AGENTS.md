# Files Content Internal AGENTS

This directory only groups `web`, `application`, `domain`, and `infra`.

## Rules

- Internal content packages are private to this module.
- Do not place direct implementation classes in this grouping directory.
