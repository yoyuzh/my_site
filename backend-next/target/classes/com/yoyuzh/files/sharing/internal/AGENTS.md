# Files Sharing Internal AGENTS

This directory only groups `web`, `application`, `domain`, and `infra`.

## Rules

- Internal sharing packages are private to this module.
- Do not place stray executable classes directly in this directory.
