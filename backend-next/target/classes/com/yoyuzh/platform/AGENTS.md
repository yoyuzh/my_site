# Platform Group AGENTS

This directory groups platform-level support domains.

## Rules

- `job` and `storage` are platform services, not generic utility bags.
- Do not place user-facing business modules directly here.
